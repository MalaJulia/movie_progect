import './App.css';
import {MoviesList} from "./components";

function App() {

    const som = () => {

    }

  return (
      <button onClick={som}>
button
      </button>,
      <MoviesList/>
  );
}

export default App;
